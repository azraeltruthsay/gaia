[project]
name = "gaia-wiki"
version = "0.1.0"
description = "GAIA internal developer documentation"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "ruff>=0.1.0",
]

[tool.ruff]
target-version = "py311"
line-length = 120
