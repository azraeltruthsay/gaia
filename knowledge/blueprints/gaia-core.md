# GAIA Service Blueprint: `gaia-core` (The Brain (Cognition))

> **Status:** 🟢 LIVE  
> **Service version:** 0.6  
> **Blueprint version:** 0.6  
> **Generated by:** `reflection`  
> **Last reflected:** 2026-03-03 15:30 UTC  

## Purpose

The cognitive engine of GAIA. Runs the full reasoning loop: intent detection, tool routing, multi-step reflection, and response generation. Deliberately CPU-only to allow gaia-prime and gaia-study to share the GPU without interrupting cognition. All inference is delegated to gaia-prime, with graceful fallback chains preserving uptime across backend failures.


**Cognitive role:** The Brain

### Design Decisions

- Model-to-model cascade routing (Nano-Refiner triage → Operator → Prime)
- CPU-only runtime enables GPU handoffs between prime and study without blocking the cognition loop
- Falls back through groq → gguf rather than hard-failing — uptime over raw capability
- gaia-web owns output routing so core remains interface-agnostic
- Four-tier memory: session (short-term), semantic codex (mid-term), vector store (long-term), prime.md checkpoint (sleep continuity)
- Guided decoding via json-architect LoRA adapter for reliable structured output from smaller models
- Sleep/wake cognitive continuity — LLM-generated checkpoint captures introspective state, consumed-sentinel prevents stale injection
- Parallel wake strategy — GPU reclaim and checkpoint load happen concurrently for faster wake
- Human-in-the-loop approval flow for destructive tool calls via gaia-mcp /request_approval

## Runtime

| Property | Value |
|----------|-------|
| Port | `6415` |
| Base image | `python:3.11-slim` |
| GPU | No |
| Health check | `curl -f http://localhost:6415/health` |
| Startup | `uvicorn gaia_core.main:app --host 0.0.0.0 --port 6415` |
| User | `${UID}:${GID}` |
| Dockerfile | `gaia-core/Dockerfile` |
| Compose service | `gaia-core` |

## Interfaces

### Inbound

- **`process_packet`** ✅ `http_rest` `/process_packet`  
  Primary cognition entry point. Receives CognitionPackets from gaia-web, runs the full reasoning loop, returns completed packet.
- **`health`** ✅ `http_rest` `/health`  
  Container health check endpoint.
- **`root`** ✅ `http_rest` `/`  
  Root endpoint — returns list of available endpoints for service discovery.
- **`status`** ✅ `http_rest` `/status`  
  Cognitive status — current state, uptime, active sessions.
- **`gpu_status`** ✅ `http_rest` `/gpu/status`  
  GPU allocation state — owned, released, or unavailable.
- **`gpu_release`** ✅ `http_rest` `/gpu/release`  
  Release GPU to orchestrator pool. Called by gaia-orchestrator.
- **`gpu_reclaim`** ✅ `http_rest` `/gpu/reclaim`  
  Reclaim GPU from orchestrator pool. Called by gaia-orchestrator.
- **`sleep_wake`** ✅ `http_rest` `/sleep/wake`  
  Wake signal from gaia-web — triggers transition from ASLEEP to WAKING.
- **`sleep_status`** ✅ `http_rest` `/sleep/status`  
  Query current sleep state (ACTIVE, DROWSY, ASLEEP, WAKING).
- **`sleep_study_handoff`** ✅ `http_rest` `/sleep/study-handoff`  
  Study handoff endpoint — orchestrator signals study cycle complete, GPU available.
- **`sleep_distracted_check`** ✅ `http_rest` `/sleep/distracted-check`  
  Check if GAIA is asleep and should return a canned/distracted response.
- **`sleep_shutdown`** ✅ `http_rest` `/sleep/shutdown`  
  Graceful shutdown — completes current cycle, writes checkpoint, enters sleep.

### Outbound

- **`prime_inference`** ✅ `http_rest` `/v1/chat/completions`  
  LLM inference requests to gaia-prime via OpenAI-compatible API.
- **`prime_health`** ✅ `http_rest` `/health`  
  Health probe to gaia-prime on boot — sets prime_available flag.
- **`mcp_dispatch`** ✅ `mcp` `—`  
  Tool execution requests dispatched to gaia-mcp via JSON-RPC.
- **`mcp_approval`** ✅ `http_rest` `/request_approval`  
  Tool approval requests sent to gaia-mcp for human-in-the-loop confirmation.
- **`orchestrator_gpu_sleep`** ✅ `http_rest` `/gpu/sleep`  
  Notify orchestrator that gaia-core is entering sleep — GPU available for study.
- **`orchestrator_gpu_wake`** ✅ `http_rest` `/gpu/wake`  
  Notify orchestrator that gaia-core is waking — request GPU reclamation.
- **`web_presence`** ✅ `http_rest` `/presence`  
  Presence updates to gaia-web — online/typing/sleeping status for Discord.

## Service Dependencies

- **`gaia-prime`** — inference · optional (fallback: `groq-api`)
- **`gaia-mcp`** — tool_execution · optional (fallback: `—`)
- **`gaia-web`** — output_routing · required
- **`gaia-orchestrator`** — gpu_lifecycle · optional (fallback: `—`)

## External API Dependencies

- **groq** — inference_fallback_when_prime_unavailable · optional
- **openai** — oracle_tier_inference · optional
- **gemini** — oracle_tier_inference · optional

## Volume Dependencies

- **gaia-shared** `rw` → `/shared` — Session state, cognitive checkpoints, prime.md sleep notes
- **knowledge** `ro` → `/knowledge` — Blueprints, semantic codex, recitable docs
- **vector_store** `rw` → `/vector_store` — FAISS vector indices for long-term memory
- **models** `ro` → `/models` — LoRA adapters (json-architect, persona weights)
- **logs** `rw` → `/logs` — Structured cognition logs, heartbeat telemetry

## Failure Modes

- 🟡 **gaia-prime unavailable**
  → Falls back to Groq API; if Groq unavailable, falls back to local GGUF model
- 🟡 **gaia-mcp unavailable**
  → Tool calls skipped; responds with capability_unavailable in packet
- 🟠 **All inference backends unavailable**
  → Returns error packet to gaia-web; session preserved for retry

## Source Files

- `gaia-core/gaia_core/main.py` _entrypoint_ [python]
- `gaia-core/gaia_core/cognition/agent_core.py` _core_logic_ [python]
- `gaia-core/gaia_core/cognition/sleep_cycle_loop.py` _sleep_lifecycle_ [python]
- `gaia-core/gaia_core/cognition/sleep_wake_manager.py` _sleep_state_machine_ [python]
- `gaia-core/gaia_core/utils/prompt_builder.py` _prompt_assembly_ [python]
- `gaia-common/gaia_common/constants/gaia_constants.json` _config_ [json]

## Internal Architecture

| Component | Description | Key Classes |
|-----------|-------------|-------------|
| **API Layer** | FastAPI entrypoint and endpoint routers. | — |
| **Cognition Engine** | Core reasoning loop. AgentCore runs multi-turn reasoning with self-improvement. | — |
| **Sleep/Wake System** | Manages ACTIVE → DROWSY → ASLEEP → WAKING lifecycle. | — |

### API Layer (`api_layer`)

FastAPI entrypoint and endpoint routers.

**Source files:**
- `gaia-core/gaia_core/main.py`

**Interface mapping:** Exposes: `process_packet`, `health`, `root`, `status`, `gpu_status`, `gpu_release`, `gpu_reclaim`, `sleep_wake`, `sleep_status`, `sleep_study_handoff`, `sleep_distracted_check`, `sleep_shutdown`

### Cognition Engine (`cognition_engine`)

Core reasoning loop. AgentCore runs multi-turn reasoning with self-improvement.

**Key functions:**
- `AgentCore.run_turn()`
- `_nano_triage()`

**Source files:**
- `gaia-core/gaia_core/cognition/agent_core.py`

### Sleep/Wake System (`sleep_system`)

Manages ACTIVE → DROWSY → ASLEEP → WAKING lifecycle.

**Source files:**
- `gaia-core/gaia_core/cognition/sleep_cycle_loop.py`
- `gaia-core/gaia_core/cognition/sleep_wake_manager.py`

### Internal Wiring

- **api_layer** → **cognition_engine**: dispatches CognitionPackets to (`CognitionPacket`)
- **api_layer** → **sleep_system**: routes sleep/wake/GPU endpoints to (`wake signal, sleep commands`)

## Blueprint Confidence

| Section | Confidence |
|---------|------------|
| Runtime | 🟢 ConfidenceLevel.HIGH |
| Contract | 🟢 ConfidenceLevel.HIGH |
| Dependencies | 🟢 ConfidenceLevel.HIGH |
| Failure Modes | 🟢 ConfidenceLevel.HIGH |
| Intent | 🟢 ConfidenceLevel.HIGH |

---
_Generated automatically from `gaia-core.yaml`. Do not edit this file directly._