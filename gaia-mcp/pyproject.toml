[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gaia-mcp"
version = "0.1.0"
description = "GAIA MCP - The Hands: Sandboxed tool execution environment"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "GAIA Project", email = "gaia@localhost"},
]
dependencies = [
    "gaia-common",
    "fastapi>=0.115.0",
    "uvicorn>=0.29.0",
    "pydantic>=2.0.0",
    "jsonschema>=4.0.0",
    "sentence-transformers>=2.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    # o9dh/saq3: pinned exact -- see gaia-core/pyproject.toml for why
    # (unpinned floors let validate.sh's fresh builds silently resolve a
    # different ruff/mypy than the long-running candidate container).
    "ruff==0.15.8",
    "mypy==1.19.1",
]

[tool.hatch.build.targets.wheel]
packages = ["gaia_mcp"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
ignore = [
    "E402",  # Module-level import not at top (intentional: conditional imports)
    "F403",  # Star imports in __init__.py (intentional re-exports)
    "F401",  # Unused imports (cleanup tracked separately)
    "F841",  # Unused local variables
    "E401",  # Multiple imports on one line
    "F541",  # f-string without placeholders
    "E701",  # Multiple statements on one line (colon)
    "E731",  # Lambda assignment
    "E741",  # Ambiguous variable name
    "F811",  # Redefinition of unused name
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = ["ignore::DeprecationWarning", "ignore::PendingDeprecationWarning"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
ignore_missing_imports = true
implicit_optional = true
check_untyped_defs = false
disallow_untyped_defs = false
warn_return_any = false
no_strict_optional = true
# o9dh/saq3: ignore_missing_imports covers modules mypy can't find at
# all; requests IS installed but ships without py.typed/stubs, a
# separate error code (import-untyped) that flag doesn't suppress.
disable_error_code = ["import-untyped"]
